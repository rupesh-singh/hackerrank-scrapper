class Problem:

    def __init__(self, name, statement, code, domain, sub_domain, language):
        self.name = name
        self.statement = statement
        self.code = code
        self.domain = domain
        self.sub_domain = sub_domain
        self.language = language
